# render.yaml
# Corrected indentation and added plan.

services:
  - type: web
    name: smart-research-assistant
    env: python
    plan: free # Explicitly set the plan to avoid defaults
    buildCommand: |
      cd frontend
      npm install
      npm run build
      cd ..
      pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
    rewrites:
      - source: /api/*
        destination: /api/*
      - source: /*
        destination: /index.html
